# LIVE STATE — recovery spine

**Status:** authoritative recovery checkpoint after `RELATIONAL_DISTINCTION_AUDIT` on 2026-08-27.

Read this first if conversational state is lost. Detailed earlier lineage remains in Git history at commit `b92d44f5b83307578c302770ebee6b87b8d13b70` and in the preserved artifacts referenced below.

## Program center

```math
\boxed{I_{\rm adaptive}\propto C_{\rm improve}}
```

Developmental loop:

```math
\boxed{\text{distinguish}\rightarrow\text{transform}\rightarrow\text{reconstitute}\rightarrow\text{continue}\rightarrow\text{distinguish better}}
```

Operational separation:

```text
METHOD
  interrogate object / constitute next distinction

PROTOCOL
  expose candidate to declared independent future
  compute C_improve
  inherit / reject

RESEARCH STATE
  preserve consequence
  localize failure
  change the next question
```

Rules:

```math
\boxed{\textbf{Revision depth must match failure depth.}}
```

```math
\boxed{\text{interesting observation}\not\Rightarrow\text{new machinery}}
```

```math
\boxed{\textbf{Does this machinery change what survives, or merely describe the machinery?}}
```

## Trace incumbent

```text
R0 = Groundup-001 / leaderboard #20
LL = 0.6020
AUC = 0.6274
```

`R0` remains frozen. **No Groundup-002 exists.**

## Trace search-state lineage

### K0

Temporal-prefix aliasing was investigated and rejected as the first collision: the competition contract supplies completed-session context rather than distinct admissible response-time prefixes.

### K1

A structural session-objective factorization limitation remains legitimate:

```math
f(s,o)=A(s)+B(o)
```

cannot express objective-conditioned session evidence `J(s,o)`.

Cheap lexical implementation 001 was tested and killed on cold-objective portability:

```text
dev seed13       C_improve = +0.000006240895
fresh session73  C_improve = +0.000011606736
cold objective52 C_improve = -0.000057729501
```

Conclusion: candidate relation remains open; lexical coverage implementation died.

### K2

Residual-collision role audit: marginal student/tutor lexical decompositions did not establish a stable separator. All bootstrap intervals crossed zero.

### K3

Relational-collision audit on 163 hard pairs / 157 sessions tested four primitive lexical-order relations. None replicated strongly enough to authorize `D2`.

### K4 — current

`RELATIONAL_DISTINCTION_AUDIT` preregistered a broader eight-relation primitive event family before outcomes were inspected.

Hard residual class `C2`:

- same session;
- opposite objective outcomes;
- exact tie in combined lexical coverage;
- exact tie in student lexical coverage;
- exact tie in tutor lexical coverage;
- absolute frozen Groundup-001 probability difference <= 0.05.

Population:

```text
163 pairs / 157 sessions
discovery:    73 pairs / 70 sessions
confirmation: 90 pairs / 87 sessions
```

Predeclared relations:

1. relevant student question -> relevant tutor response;
2. relevant tutor question -> relevant student response;
3. S(rel) -> T(rel/nonrel) -> S(rel) return contrast;
4. S(nonrel) -> T(rel/nonrel) -> S(rel) entry contrast;
5. cross-role relevant adjacency density;
6. relevance segment fragmentation / re-entry;
7. student-relevant -> next tutor-relevant proximity;
8. tutor-relevant -> next student-relevant proximity.

Inference was session-clustered. Each split used two-sided sign-flip permutation tests with Holm correction across all eight relations. `D2` authorization required Holm-adjusted p<0.05 in discovery and confirmation with the same nonzero sign.

**Result:** no relation was established.

Strongest discovery-looking relation was relevant student question -> relevant tutor response:

```text
discovery mean session delta = +0.053401
95% CI = [-0.007143, +0.118707]
Holm p = 0.855023

confirmation mean session delta = +0.014368
95% CI = [-0.034483, +0.066092]
Holm p = 1.0
```

All other familywise-adjusted p-values were 1.0 in both splits.

Therefore:

```text
ESTABLISHED D2: none
R state: R0 unchanged
Groundup-002: not authorized
K3 -> K4
```

Earned K4 statement:

> For this hard residual population, the tested family of simple objective-conditioned event relations—question/response relevance, S->T->S return/entry contrasts, cross-role relevance adjacency, relevance fragmentation/re-entry, and directional relevant-turn proximity—did not identify a replicating separator.

This does **not** establish that interaction, sequence, or the session-objective dependency is irrelevant. It establishes only that these preregistered primitive measurements failed to identify `D2`.

Next legal Trace operation remains **DISTINGUISH**, not TRANSFORM.

## Audit artifacts

Preserve under:

```text
artifacts/trace-relational-distinction-audit-001/
  RELATIONAL_DISTINCTION_AUDIT_PREREG.md
  relational_distinction_audit.py
  RELATIONAL_DISTINCTION_AUDIT.json
  RELATIONAL_DISTINCTION_AUDIT.md
```

Preregistration SHA-256:

```text
b71a988d87859913e345896c089d56c5144df87eaf9d42e30de6d806923835f1
```

Result JSON SHA-256:

```text
fd8b6c874d0094a35d89191888c4d70aa56a90275dca20feac101757c656a24a
```

A deterministic rerun reproduced the exact result JSON hash.

## Frozen protocol state

`adaptive-loop-protocol-001` remains frozen under:

```text
artifacts/adaptive-loop-protocol-001/
```

Earned claim only:

> It can enforce bookkeeping and survival conditions of an already-constituted adaptive transition without assuming the domain ontology.

No claim of sufficiency for intelligence, adaptation, corrigibility, causal identification, or useful improvement.

Two-domain conformance remains preserved under:

```text
artifacts/adaptive-loop-conformance-001/
```

It passed Trace + symbolic program repair without a protocol edit. `protocol-002` is not authorized.

## Open K boundary

PROTOCOL-001's code-level `K` is an evaluated-transition ledger. Trace `K4` also contains diagnostic-only updates that changed the next question without Candidate + Consequence transitions.

Still OPEN:

1. narrow K: survival-transition ledger belongs to core; broader research/search state stays outside;
2. full K: core should somehow own diagnostic-only research-state updates.

Do not patch this boundary without cross-domain evidence.

## Persistence invariant

> **A meaningful change in authoritative operative state or correction-relevant research state is not reconstituted until its minimal recovery record is persisted outside the conversation.**

Persist correction-relevant lineage, not scratch accumulation.

## Current claim ceiling / allowed work

Earned:

- Groundup-001 remains frozen #20 incumbent;
- Trace search state is now `K4`;
- no `D2` is identified;
- no Groundup-002 is authorized;
- eight-family relational distinction audit is a negative result with preregistration, multiplicity control, and independent confirmation;
- PROTOCOL-001 remains frozen;
- METHOD / PROTOCOL / RESEARCH STATE remain separate.

Not earned:

- that event interaction is absent;
- that session-objective dependency is useless;
- a new Trace representation;
- protocol-002;
- full K ownership by the protocol;
- general intelligence/corrigibility claims.

Next legal Trace operation:

```math
\boxed{\textbf{DISTINGUISH}}
```

The center remains:

```math
\boxed{\textbf{Does adaptive feedback make the future better than it otherwise would have been?}}
```
